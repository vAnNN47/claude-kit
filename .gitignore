# Secrets — never commit
settings.local.json
*.local
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db

# Marketplace skills (skills.sh) — materialized files + symlinks are machine-specific.
# skills-lock.json IS tracked (the portable manifest); restore with `npx skills add`.
/.agents/
